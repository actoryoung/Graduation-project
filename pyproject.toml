[project]
name = "biyesheji"
version = "0.1.0"
description = "Multimodal Emotion Analysis System"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "paper-search-mcp>=0.1.3",
    "torch>=2.0.0",
    "transformers>=4.30.0",
    "numpy>=1.24.0",
]
